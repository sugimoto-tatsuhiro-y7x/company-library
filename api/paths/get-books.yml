get:
  description: "本一覧を取得するエンドポイント"
  operationId: books
  tags:
    - "books"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/Book.yml"
          example:
            $ref: "../examples/bookData.json"
